'use client';

import { useMemo, useState } from 'react';

const tiers=[{key:'call',label:'Call Ready',hint:'Strong fit + usable contact path'},{key:'qualified',label:'Qualified',hint:'Good importer, needs contact research'},{key:'research',label:'Research',hint:'Potential fit, verify before outreach'},{key:'replace',label:'Replace',hint:'Weak fit / insufficient evidence'}];
const first=v=>Array.isArray(v)&&v.length?(v[0]?.key??v[0]):'—';
function normalize(body){
  const root=body?.data?.data?.data ?? body?.data?.data ?? body?.data ?? body;
  const rows=Array.isArray(root)?root:[];
  return rows.map((x,i)=>({
    id:x.company_link||x.key||i, company:x.key||x.name||'Unknown importer', country:x.company_country||'—', shipments:x.total_shipments??x.doc_count??'—',
    address:first(x.company_address), website:first(x.company_website), phone:x.company_main_phone_number||first(x.company_contact_info?.phone_numbers), email:first(x.company_contact_info?.emails),
    product:first(x.product_description), hs:first(x.hs_code), port:first(x.port_of_entry), raw:x
  }));
}
function matches(r,q){if(!q)return true;const s=q.toLowerCase();return [r.company,r.address,r.website,r.product,r.hs,r.port,r.email,r.phone].some(v=>String(v||'').toLowerCase().includes(s));}
export default function Home(){
 const[query,setQuery]=useState('');const[rows,setRows]=useState([]);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const[bucket,setBucket]=useState({});const[selected,setSelected]=useState(null);const[credits,setCredits]=useState(null);
 async function search(e){e?.preventDefault();setBusy(true);setError('');try{const qs=new URLSearchParams({path:'/api/importers',pageSize:'50'});const r=await fetch('/api/importyeti?'+qs,{cache:'no-store'});const b=await r.json();if(!r.ok||!b.ok)throw new Error(b?.data?.message||b?.error||'Importer search failed');setCredits(b?.data?.creditsRemaining??null);const all=normalize(b);setRows(all.filter(x=>matches(x,query.trim())));}catch(e){setError(String(e.message||e));}finally{setBusy(false)}}
 const counts=useMemo(()=>tiers.reduce((a,t)=>({...a,[t.key]:Object.values(bucket).filter(v=>v===t.key).length}),{}),[bucket]);
 return <main className="shell"><style>{`*{box-sizing:border-box}body{margin:0;background:#f5f7fa;color:#152033;font-family:Inter,system-ui,sans-serif}.shell{max-width:1200px;margin:auto;padding:30px 18px 70px}.top{display:flex;justify-content:space-between;gap:18px;align-items:flex-end}.eyebrow{font-size:12px;font-weight:800;letter-spacing:.12em;color:#51617a;text-transform:uppercase}h1{margin:5px 0 4px;font-size:34px}.sub,.meta{color:#718096}.sub{margin:0}.badge{background:#e8fff2;color:#087443;border:1px solid #b9efd0;padding:8px 12px;border-radius:99px;font-weight:750;font-size:13px}.search{display:flex;gap:10px;margin:25px 0 16px;background:white;padding:12px;border:1px solid #dce3eb;border-radius:14px}.search input{flex:1;border:0;outline:0;font-size:16px;padding:10px}.search button{border:0;background:#152033;color:#fff;border-radius:9px;padding:11px 18px;font-weight:750}.tiers{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:16px}.tier{background:#fff;border:1px solid #dce3eb;border-radius:12px;padding:14px}.tier strong,.tier span{display:block}.tier span,.meta{font-size:12px;margin-top:4px}.count{float:right;background:#eef2f7;padding:3px 8px;border-radius:99px}.panel{background:#fff;border:1px solid #dce3eb;border-radius:14px;overflow:hidden}.row{display:grid;grid-template-columns:1.7fr 1.15fr .55fr 1.5fr;gap:12px;padding:15px 18px;border-top:1px solid #edf0f4;align-items:center}.row:first-child{border-top:0}.company{font-weight:800;cursor:pointer}.contact{font-size:13px}.ship{text-align:right;font-weight:800}.actions{display:flex;gap:4px;flex-wrap:wrap}.actions button{border:1px solid #d4dbe5;background:#fff;border-radius:7px;padding:6px 7px;font-size:10px;font-weight:750}.actions button.active{background:#152033;color:#fff}.empty{padding:45px;text-align:center;color:#718096}.error{background:#fff0f0;color:#a12424;padding:12px;border-radius:10px;margin-bottom:12px}.drawer{margin-top:16px;background:#111827;color:#eef2ff;border-radius:14px;padding:20px}.drawer pre{white-space:pre-wrap;overflow:auto;font-size:12px;color:#cbd5e1}.close{float:right;background:#334155;color:white;border:0;border-radius:7px;padding:6px 10px}@media(max-width:760px){.top{align-items:flex-start;flex-direction:column}.tiers{grid-template-columns:1fr 1fr}.row{grid-template-columns:1fr}.ship{text-align:left}h1{font-size:28px}}`}</style>
 <header className="top"><div><div className="eyebrow">Business Tax Advisers · Importer Intelligence</div><h1>BTA Importer Explorer</h1><p className="sub">Live U.S. importer data → qualification → call-ready pipeline.</p></div><div className="badge">● LIVE ImportYeti {credits!==null?`· ${credits} credits`:''}</div></header>
 <form className="search" onSubmit={search}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Filter company, product, HS code, port, contact…"/><button disabled={busy}>{busy?'Loading live data…':'Load & Filter Importers'}</button></form>{error&&<div className="error">{error}</div>}
 <section className="tiers">{tiers.map(t=><div className="tier" key={t.key}><b className="count">{counts[t.key]||0}</b><strong>{t.label}</strong><span>{t.hint}</span></div>)}</section>
 <section className="panel">{rows.length===0?<div className="empty"><b>Live ImportYeti connection ready.</b><br/>Load importers or enter a filter above.</div>:rows.map(r=><div className="row" key={r.id}><div><div className="company" onClick={()=>setSelected(r)}>{r.company}</div><div className="meta">{r.address}</div><div className="meta">{r.product} · HS {r.hs}</div></div><div className="contact"><b>{r.website}</b><div>{r.phone}</div><div className="meta">{r.email}</div></div><div className="ship">{r.shipments}<div className="meta">shipments</div></div><div className="actions">{tiers.map(t=><button type="button" className={bucket[r.id]===t.key?'active':''} key={t.key} onClick={()=>setBucket({...bucket,[r.id]:t.key})}>{t.label}</button>)}</div></div>)}</section>
 {selected&&<section className="drawer"><button className="close" onClick={()=>setSelected(null)}>Close</button><h2>{selected.company}</h2><p>{selected.product} · HS {selected.hs} · {selected.port}</p><p>{selected.phone} · {selected.email} · {selected.website}</p><pre>{JSON.stringify(selected.raw,null,2)}</pre></section>}
 </main>;
}
